1) Указываем объявление пропсов в обобщённом типе (FC из React) в дженерике
2) Передаём в функцию объект заданного типа
3) Создаём файлик types, в котором будем указывать глобальные типы, которые создаём
4) Идём на сайт JSONplaceholder, там есть фейковое апи, можно отправлять запросы и получать данные. Смотрим что юзеры там имеют
5) map-им юзеров, не забываем про key

6) Асинхронные запросы axios (устанавливаем)
7) useState<IUsers[]>([])
8) axios.get<IUser[]> //пишем тип того, что ожидаем получить от сервера

9)универсальный компонент : Для того чтобы корректо работать с дженериками,
	                                       нужно создать компонент через объявление обычной функции.
			   export const function<T>(props:ListProps<T>){return()}
10)<T> = любой тип
11)Типизация событий. MouseEvents, DragEvents, ChangeEvents (Управляемы импут)
12) Неуправляемый импут (useRef) типизируем
13) Типизация ReactRouterDom : 